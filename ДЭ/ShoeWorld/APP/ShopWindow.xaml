<Window x:Class="APP.ShopWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:APP"
        mc:Ignorable="d"
        Title="А" MinHeight="450" MinWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Button x:Name="AddProductButton" Content="Добавить" Grid.Column="1" Grid.Row="2" VerticalAlignment="Bottom" Click="AddProductButton_Click"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Grid.Column="0" x:Name="FilterStackPanel">
            <TextBlock Text="Поиск: "/>
            <TextBox x:Name="SearchTextBox" Width="200" TextChanged="SearchTextBox_TextChanged"  />
            <ComboBox x:Name="FilterButton" Width="250" SelectionChanged="FilterButton_SelectionChanged">
                <ComboBoxItem Content="Количество на складе по убыванию" x:Name="PriceDown"/>
                <ComboBoxItem Content="Количество на складе по возрастанию" x:Name="PriceUp"/>
            </ComboBox>
            <ComboBox x:Name="FilterManufacturerButton" Width="150" DisplayMemberPath="Name" SelectionChanged="FilterManufacturerButton_SelectionChanged"/>
            <Button x:Name="CancelButton" Content="Сбросить" Click="CancelButton_Click" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Column="1">
            <Label x:Name="RoleLabel"/>
            <TextBlock x:Name="FullNameText"   VerticalAlignment="Center"/>
            <Button x:Name="ExitButton" Content="Выйти"  Click="ExitButton_Click"/>
        </StackPanel>
        <ListView  x:Name="ProductsListView" ScrollViewer.VerticalScrollBarVisibility="Auto" Grid.Row="1" Grid.ColumnSpan="2">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            Padding="{TemplateBinding Padding}">
                                    <!-- Единственный дочерний элемент -->
                                    <ContentPresenter />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Amount}" Value="0">
                            <Setter Property="Background" Value="LightBlue"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding HasBigDiscount}" Value="True">
                            <Setter Property="Background" Value="#2E8B57"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="150"/>
                        </Grid.ColumnDefinitions>
                        <Image Source="/Images/shoeimg.png" Width="auto" Height="auto" />
                        <StackPanel Grid.Column="1" Margin="20,20,20,20">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding GenderDisplay}" FontSize="20"/>
                                <TextBlock Text=" | " FontSize="20"/>
                                <TextBlock Text="{Binding Name}" FontSize="20"/>
                            </StackPanel>
                            <TextBlock><Run Text="Описание: " FontSize="16"/><Run Text="{Binding Description}" FontSize="16"/></TextBlock>
                            <TextBlock><Run Text="Производитель: " FontSize="16"/><Run Text="{Binding Manufacturer.Name}" FontSize="16"/></TextBlock>
                            <TextBlock><Run Text="Поставщик: " FontSize="16"/><Run Text="{Binding Supplier.Name}" FontSize="16"/></TextBlock>
                            <TextBlock x:Name="Oldprice"  Text="{Binding Price, StringFormat='{}{0} ₽'}" Foreground="Red" TextDecorations="Strikethrough" Visibility="Collapsed"/>
                            <TextBlock x:Name="CurrentPrice" Text="{Binding Price, StringFormat='{}{0} ₽'}" Foreground="Black" Margin="8,0,0,0"/>
                            <TextBlock Text="Еденица измерения: шт." FontSize="16"/>
                            <TextBlock><Run Text="Осталось на складе: " FontSize="16"/><Run Text="{Binding Amount}" FontSize="16"/></TextBlock>
                        </StackPanel>

                        <Label  Grid.Column="2" HorizontalAlignment="Center"  VerticalAlignment="Center" Content="{Binding Discount}" FontSize="64">
                            <Label.Style>
                                <Style TargetType="Label">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding BigDiscount}" Value="true">
                                            <DataTrigger.Setters>
                                                <Setter Property="Foreground" Value="#2E8B57"/>
                                            </DataTrigger.Setters>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Label.Style>
                        </Label>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>

</Window>
