<Window x:Class="Test1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Test1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
   
    

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Левая панель -->
        <Border Background="White" Padding="20">
            <StackPanel>
                <TextBlock Text="🌿 Eco Shop" FontSize="20" FontWeight="Bold" Margin="0,0,0,30"/>
                <TextBlock Text="Категории" FontWeight="SemiBold" Margin="0,0,0,10"/>
                <Button Content="Все товары" Style="{StaticResource {x:Type Button}}" Margin="0,0,0,5"/>
                <Button Content="Быт" Margin="0,0,0,5"/>
                <Button Content="Уход" Margin="0,0,0,5"/>
                <Button Content="Еда" />
            </StackPanel>
        </Border>

        <!-- Основная область -->
        <ScrollViewer Grid.Column="1" Margin="20">
            <StackPanel>
                <!-- Кнопки верхней панели -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,20">
                    <Button Content="🛒 Корзина" Command="{Binding GoToCartCommand}" 
                            Background="#2E7D32" Foreground="White" Padding="12,6" Margin="0,0,10,0" />
                    <Button Content="👤 Профиль" Command="{Binding GoToProfileCommand}" 
                            Background="#1976D2" Foreground="White" Padding="12,6" />
                </StackPanel>

                <!-- Заголовок -->
                <TextBlock Text="Все товары" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
                <TextBlock Text="Экологичные и безопасные для планеты" Foreground="Gray" />

                <!-- Сетка товаров -->
                <ItemsControl ItemsSource="{Binding Products}" Margin="0,20,0,0">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel ItemWidth="240" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" Margin="10" Padding="12" CornerRadius="10" 
                                    BorderThickness="1" BorderBrush="#E0E0E0" Width="220">
                                <StackPanel>
                                    <Image Source="{Binding ImageUrl}" Height="140" Stretch="UniformToFill" />
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Margin="0,8,0,4"/>
                                    <TextBlock Text="{Binding Description}" TextWrapping="Wrap" FontSize="12" Foreground="#555" />
                                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                        <TextBlock Text="{Binding Price, StringFormat={}{0:C}}" FontWeight="Bold" Foreground="#2E7D32" />
                                        <TextBlock Text="{Binding Stock, StringFormat= (в наличии: {0})}" FontSize="11" Foreground="Gray" Margin="8,0,0,0"/>
                                    </StackPanel>
                                    <Button Content="В корзину" Margin="0,10,0,0" Background="#4CAF50" Foreground="White" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>

